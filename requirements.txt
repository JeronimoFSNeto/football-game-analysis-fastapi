git+https://github.com/JeronimoFSNeto/llm-orchestrator.git@main#egg=llm-orchestrator
# Resolução de conexão TCP com fallback Happy Eyeballs (RFC 6555) para asyncio
aiohappyeyeballs==2.6.1
# Cliente/servidor HTTP assíncrono
aiohttp==3.13.3
# Gerenciamento de callbacks via signals para aiohttp
aiosignal==1.4.0
# Geração de documentação a partir de annotations
annotated-doc==0.0.4
# Tipos auxiliares para uso com typing.Annotated (validações)
annotated-types==0.7.0
# SDK oficial da Anthropic (Claude) para chamadas de LLM
anthropic==0.79.0
# Camada de compatibilidade para asyncio (trio/asyncio)
anyio==4.12.1
# Definição de classes com atributos (usado internamente por aiohttp, etc.)
attrs==25.4.0
# Bundle de certificados raiz para validação SSL/TLS
certifi==2026.1.4
# Interface Foreign Function para chamadas C (dependência de cryptography)
cffi==2.0.0
# Detecção de encoding de caracteres para requests/aiohttp
charset-normalizer==3.4.4
# Criação de interfaces de linha de comando (base do Typer)
click==8.3.1
# Funções criptográficas (hashing, criptografia, assinaturas)
cryptography==46.0.5
# Detecção de informações do sistema operacional
distro==1.9.0
# Parser de docstrings Python (Google, NumPy, reStructuredText)
docstring_parser==0.17.0
# Geração de UUIDs de alta performance
fastuuid==0.14.0
# Lock de arquivos cross-platform (evita acessos concorrentes)
filelock==3.24.2
# Lista imutável (dependência de aiohttp/aiosignal)
frozenlist==1.8.0
# Abstração de filesystem (local, S3, GCS, etc.) – usado pelo HuggingFace Hub
fsspec==2026.2.0
# Ferramenta para reescrever histórico de repositórios Git
git-filter-repo==2.47.0
# Tipos gerados para a API Google AI (Generative Language / Gemini)
google-ai-generativelanguage==0.6.15
# Biblioteca core para APIs Google (retry, auth, transporte gRPC/REST)
google-api-core==2.29.0
# Cliente Python para APIs Google via REST (Discovery API)
google-api-python-client==2.190.0
# Autenticação e autorização para serviços Google
google-auth==2.48.0
# Integração google-auth com httplib2
google-auth-httplib2==0.3.0
# SDK de alto nível do Google Gemini (GenAI)
google-genai==1.63.0
# Tipos protobuf comuns usados pelas APIs Google
googleapis-common-protos==1.72.0
# Framework gRPC para comunicação RPC de alta performance
grpcio==1.78.0
# Mapeamento de status gRPC para erros Google API
grpcio-status==1.71.2
# Implementação HTTP/1.1 de baixo nível (base do httpcore)
h11==0.16.0
# Backend de transferência otimizado para o HuggingFace Hub
hf-xet==1.2.0
# Biblioteca HTTP core (base do httpx)
httpcore==1.0.9
# Cliente HTTP (utilizado por google-auth e google-api-python-client)
httplib2==0.31.2
# Cliente HTTP moderno com suporte a async (usado pelos SDKs OpenAI/Anthropic)
httpx==0.28.1
# Cliente para o HuggingFace Hub (download/upload de modelos e datasets)
huggingface_hub==1.4.1
# Suporte a Internationalized Domain Names (IDNA)
idna==3.11
# Acesso a metadados de pacotes instalados (backport)
importlib_metadata==8.7.1
# Parser de arquivos .ini (usado pelo pytest)
iniconfig==2.3.0
# Motor de templates (usado para renderização de prompts)
Jinja2==3.1.6
# Parser JSON rápido escrito em Rust (usado pelo pydantic/openai)
jiter==0.13.0
# Validação de dados JSON contra schemas
jsonschema==4.26.0
# Especificações do JSON Schema (meta-schemas)
jsonschema-specifications==2025.9.1
# Proxy unificado para chamadas a diversos LLMs (OpenAI, Anthropic, Gemini, etc.)
litellm==1.81.12
# Parser Markdown (base do rich para renderização de MD no terminal)
markdown-it-py==4.0.0
# Escape seguro de strings HTML (dependência do Jinja2)
MarkupSafe==3.0.3
# Parser de URLs em Markdown
mdurl==0.1.2
# Dicionário com suporte a múltiplos valores por chave (usado por aiohttp)
multidict==6.7.1
# SDK oficial da OpenAI para chamadas de LLM (GPT, etc.)
openai==2.21.0
# Utilitários de versionamento e empacotamento Python
packaging==26.0
# Sistema de plugins (utilizado pelo pytest)
pluggy==1.6.0
# Cache de propriedades assíncronas (dependência de yarl/aiohttp)
propcache==0.4.1
# Wrapper Pythonic sobre mensagens protobuf
proto-plus==1.27.1
# Serialização de dados com Protocol Buffers (comunicação gRPC)
protobuf==5.29.6
# Codificação/decodificação ASN.1 (usado em certificados e auth)
pyasn1==0.6.2
# Módulos ASN.1 para padrões como PKCS, X.509
pyasn1_modules==0.4.2
# Parser de código C (dependência do cffi)
pycparser==3.0
# Validação de dados e serialização com type hints
pydantic==2.12.5
# Core em Rust do Pydantic (validação de alta performance)
pydantic_core==2.41.5
# Syntax highlighting de código (usado pelo rich)
Pygments==2.19.2
# Parser de expressões gramaticais (dependência de httplib2)
pyparsing==3.3.2
# Framework de testes Python
pytest==9.0.2
# Plugin pytest para testes assíncronos (async/await)
pytest-asyncio==1.3.0
# Carregamento de variáveis de ambiente a partir de arquivos .env
python-dotenv==1.2.1
# Parser e emitter de YAML
PyYAML==6.0.3
# Cliente Python para Redis (cache, filas, pub/sub)
redis==7.1.1
# Resolução de referências JSON (dependência do jsonschema)
referencing==0.37.0
# Expressões regulares com suporte avançado (Unicode, etc.)
regex==2026.1.15
# Biblioteca HTTP clássica para requisições web
requests==2.32.5
# Renderização rica no terminal (cores, tabelas, progress bars, markdown)
rich==14.3.2
# Estruturas de dados persistentes (dependência do referencing/jsonschema)
rpds-py==0.30.0
# Criptografia RSA (assinaturas, verificação – dependência do google-auth)
rsa==4.9.1
# Detecção de shell do usuário (dependência do typer)
shellingham==1.5.4
# Detecção de biblioteca async em uso (asyncio vs trio)
sniffio==1.3.1
# Retry automático com backoff configurável
tenacity==9.1.4
# Tokenizador BPE da OpenAI (contagem de tokens para GPT)
tiktoken==0.12.0
# Tokenizadores rápidos do HuggingFace (escritos em Rust)
tokenizers==0.22.2
# Barras de progresso para loops e iteráveis
tqdm==4.67.3
# Framework CLI moderno baseado em type hints (sobre click)
typer==0.23.1
# Versão slim do Typer (sem dependências extras)
typer-slim==0.23.1
# Inspeção de type hints em tempo de execução
typing-inspection==0.4.2
# Backport de recursos do módulo typing para versões mais antigas do Python
typing_extensions==4.15.0
# Expansão de templates URI (RFC 6570) – usado por APIs Google
uritemplate==4.2.0
# Cliente HTTP de baixo nível (dependência do requests)
urllib3==2.6.3
# Comunicação WebSocket (bidirecional, async)
websockets==15.0.1
# Parser e construtor de URLs (dependência do aiohttp)
yarl==1.22.0
# Acesso a arquivos dentro de zip/pacotes (backport de zipimport)
zipp==3.23.0
